import styled from 'styled-components';
import { overpass } from '../../styles/variables';

const Title = styled.h1`
    font-family: ${overpass};
    font-size: 58px;
    padding-top: 110px;
`;

export default Title;